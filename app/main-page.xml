<Page xmlns="http://schemas.nativescript.org/tns.xsd"
      xmlns:nativescript-fresco="nativescript-fresco" 
      xmlns:Card="nativescript-cardview"
      loaded="onLoaded">

    <GridLayout rows="44, 88, *" columns="*">
        <TextView row="0" text="Choose a date to show photos taken from Curiosity rover" class="textview" editable="false" />
        
        <DatePicker row="1" day="06" month="08" year="2012"></DatePicker>
        
        <ListView row="2" items="{{ dataItems }}" class="listview" loaded="onListLoaded" itemLoading="onItemLoading" itemTap="onItemTap">
          <ListView.itemTemplate>
            <Card:CardView class="cardStyle" margin="10" elevation="40" radius="5">
                <GridLayout rows="44, *" columns="2*, *">
                    <Label row="0" col="0" text="{{ cameraName }}" class="camera" textWrap="true" />
                    <Label row="0" col="1" text="{{ 'taken on:' + earthDate }}" class="date" textWrap="true" />
                    <nativescript-fresco:FrescoDrawee row="1" col="0" colSpan="2"
                                                      verticalAlignment="top"
                                                      showProgressBar="true"
                                                      aspectRatio="2"
                                                      placeholderImageUri="res://icon"
                                                      imageUri="{{ imageUri }}"/>
                </GridLayout>
            </Card:CardView>                                      
          </ListView.itemTemplate>
      </ListView>
    </GridLayout>
</Page>